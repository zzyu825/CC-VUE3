import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')

// new Vue(option).$mount('#app');
// Vue.use();

// const app = createApp(App); // vue应用对象
// console.log(app);
// app.use()
